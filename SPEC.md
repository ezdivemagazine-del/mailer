# 通路開發信系統 — 產品規格書 (SPEC)

> 易潛企業 ‧ 潛水產業總代理部門 ‧ v1.0
> 本文件供 Claude Code 開發使用。撰寫者為 PM（不具前後端開發能力，有 UI 美感）。

---

## 給 Claude Code 的開場說明（請先讀）

- **專案目標**：打造一套**全公司可共用的「行銷信件自動化平台」**。首版聚焦潛水總代理部門的通路開發信，但架構須設計成可擴展到易潛企業其他部門共用。使用者用自然語言下指令，AI 產出多語言行銷信件，經人工確認後分批寄送，回覆者轉交業務跟進。
- **部署環境（重要限制）**：首版部署於公司內部 QNAP NAS，型號 **TS-431X2、8GB RAM、32 位元 ARM 架構（Cortex-A15）**，透過 Container Station (Docker) 執行。技術選型須選用**同時支援 ARM 與 x86 的元件，並打包成多架構映像檔（multi-arch，含 arm32v7/armhf 與 amd64）**，使首版能在此 ARM NAS 運行、未來無痛遷移到 x86 機器或雲端。優先選輕量、有 ARM 支援的技術堆疊。僅供公司內網，不對外開放。
- **委託人背景**：PM 角色，不會寫程式碼，但有 UI 美感。請用「PM 看得懂」的方式溝通技術決策，主動補齊技術細節，每階段提供可驗收成果。
- **開發策略**：階段式交付。先做 MVP 驗證核心體驗，再逐步加排程、權限、附件。每階段結束都要能實際執行給 PM 看。

---

## 1. 商業目標

代理 Cressi、Seac、Alpha Oceano 三品牌進台灣，發展潛水店通路。痛點：沒有業務人力主動開發。

本系統定位為**全公司共用的行銷信件自動化平台**：首版先解決潛水總代理部門的通路開發信、驗證核心價值；架構設計成可橫向擴展，讓易潛企業其他部門（各有不同寄送對象與訴求）未來也能共用同一套系統。商業上是「自動化開發前哨」，唯一成功指標是讓收信對象「舉手回應」（回信或加 LINE）。後端已有業務承接，故每封信目標是釣出興趣訊號，而非當場成交。

## 2. 使用者流程（五步）

1. **輸入指令** — 任意語言輸入（老闆習慣英文），選擇產出語言。
2. **AI 產出信件** — 產出標題與內文草稿。
3. **對話式修改** — 多輪對話要求修改，AI 記住前文重出，直到滿意。
4. **貼名單＋設排程** — 貼 email 清單，設定分批節奏與頻率。
5. **按發送** — 後端背景分批寄出；關閉瀏覽器/電腦不中斷。

## 3. 功能需求

### 3.1 AI 寫稿模組（步驟 1–3）
- 輸入框接受任意語言自由輸入。
- 產出語言選擇器：固定繁中／簡中／英文三選項，預設繁中，建議三按鈕並排。
- 簡中須為面向中國大陸通路的商務語氣，非單純繁簡轉換。
- 一次產出「標題」與「內文」，分區顯示。
- 對話式修改：保留對話歷史並一併傳給 AI，理解相對指令（「再正式一點」）。
- 切換語言後按生成 = 以新語言重新產出，非翻譯現稿。
- （選配）內建 Cressi/Seac/Alpha Oceano 主題輪替範本。

### 3.2 收件人與發送模組（步驟 4–5）
- 收件人：貼 email 清單（一行一個）或上傳 CSV。初期約 300 筆。
- 寄件者選擇：發送前可選已串接的寄件信箱。
- **分批節流（硬性）**：可設「每 N 分鐘寄 M 封」（如 5 分鐘 50 封），降低進垃圾匣風險。
- **背景執行＋斷點續傳**：任務後端背景執行，前端關閉仍續；記錄進度，重啟可接續。
- （進階）週期性排程：如每兩週自動發一次，後端排程器觸發。
- 發送結果記錄：每封成功／失敗／退信狀態。

### 3.3 帳號與權限模組
- 角色：至少 admin 與一般成員。供 2–3 部門共用。
- admin：建立帳號、停用/刪除帳號、重設密碼。離職員工以停用帳號阻擋（停用優於改密碼）。
- **密碼雜湊加密儲存，任何人含 admin 不可見原始密碼，僅能重設。嚴禁明碼。**
- 「能串接信箱」與「能用哪個寄件信箱」受 admin 控管，避免跨部門誤用。

### 3.4 寄件信箱串接模組
- 可串接並管理多個寄件來源（Gmail、alimail、專用推送服務）。
- **串接後測試發送（硬性）**：寄測試信確認可送達，通過才允許正式使用。
- **憑證加密保管於後端，絕不暴露前端或畫面。**

### 3.5 圖片與附件
- 內文嵌圖：圖片放網路空間、信中連結載入，避免信件過大。
- 附件：支援但開發信預設不夾。
- 開發信最佳實務：預設改用內文下載連結（如「點此下載 Cressi 經銷價目表」），護送達率並可追蹤點擊作為熱線索。

## 4. 技術架構與部署

前後端整合網頁應用，整包部署於 QNAP NAS 的 Container Station (Docker)。前端供操作，後端負責 AI 呼叫、憑證保管、排程、分批發送、進度記錄。使用者經內網以瀏覽器存取。

- **前端**：步驟 1–5 的操作介面。
- **後端**：常駐服務 — 呼叫 AI、保管憑證、排程器（時間觸發）、分批節流寄送、記錄進度。
- **資料持久化**：帳號、憑證、名單、任務與進度存資料庫；資料須掛載到容器外的 NAS 實體路徑，避免容器刪除遺失。
- **容器設定**：提供 `docker-compose.yml`，含 `restart: always`；附 PM 友善的逐步部署說明與常見錯誤排查（埠號衝突、路徑掛載、ARM 映像檔不相容）。
- **多架構相容（重要）**：首版部署機為 32 位元 ARM（TS-431X2），但因系統定位為全公司平台、未來可能換 x86 或上雲，須選用同時有 ARM 與 x86 版本的元件，打包成多架構映像檔（multi-arch，含 arm32v7/armhf 與 amd64），同一包到處可跑。前提：每個元件本身都要有 ARM 版（多架構打包不會讓只有 x86 的元件變得能在 ARM 上跑）。官方提醒第三方容器在 32 位元 ARM 上可能不穩，須選輕量元件並做穩定性測試。若實測不穩，啟用備案（發送外包給專用推送服務，或改用 x86 機器）。

### 4.1 寄信出口策略（重要）
| 用途 | 建議出口 | 設定 |
|---|---|---|
| 少量／測試 | Gmail 或 alimail SMTP | alimail：`smtp.qiye.aliyun.com`、SSL 埠 465、帳號全名、三方客戶端安全密碼；需先請 IT 開通三方客戶端 |
| 批量開發信 | 專用郵件推送服務 | alimail 官方不建議用企業信箱批量發信（傷信譽）；改用阿里雲 DirectMail 或同類，信譽獨立 |

寄送模組須可抽換寄件出口，由 admin 決定哪個信箱配哪種用途。批量發送預設指向專用推送服務。

## 5. 非功能需求與安全
- 預設僅內網存取；外網操作非首版範圍。
- 憑證／密碼一律加密，不入前端與日誌。
- 正式發送前須完成公司網域驗證（DNS），否則易進垃圾匣（IT 協助、部署前置）。
- 保留「按發送」人工確認關卡（法律責任在寄件公司）。首封建議自我介紹＋徵詢是否願意持續收信。
- 須附部署文件與錯誤排查，讓 PM 在 Claude Code 協助下自行部署維護。

## 6. 開發階段（里程碑）
0. **前置確認** — 部署機已確認 TS-431X2 / 8GB / 32 位元 ARM（記憶體達標，架構為 ARM 有不穩風險）。技術選型一律確認 ARM 相容性；確認 AI API 與金鑰；備妥備案（發送外包或換 x86 機）。
1. **AI 寫稿 MVP** — 步驟 1–3，先跑起來驗證體驗。
2. **發送核心** — 名單、信箱串接＋測試、分批節流、背景發送＋進度。
3. **權限與排程** — admin/成員權限、寄件權限綁定、週期排程。
4. **內容增強與上線** — 嵌圖、附件/連結策略；打包 compose 部署到 NAS，附 PM 友善部署文件。

## 7. 開放問題（需 PM/IT 確認）
- 部署機已確認 TS-431X2 / 8GB / 32 位元 ARM。待確認：ARM 實際穩定度，不穩則啟用備案。
- AI 串接方式與金鑰來源。
- 批量發送用哪家郵件推送服務。
- 公司網域驗證（DNS）負責人與時程。
- 共用部門與預估人數。

---
_備註：成本、方案、平台功能會隨時間變動，採用前請再確認當下官方資訊。標示「進階／選配」者可延後實作。_
