# 易潛企業開發信系統 — 部署說明

> 適用對象：PM（不具開發背景），搭配 Claude Code 操作。
> 部署目標：QNAP TS-431X2（32-bit ARM，8GB RAM）的 Container Station。

---

## 第一次部署（從零開始）

### 步驟 1：確認 NAS 已開啟 Container Station

1. 登入 QNAP 管理介面（通常是 `http://NAS的IP位址`）
2. 打開 **Container Station**（若沒看到，請至 App Center 安裝）
3. 確認 Container Station 版本 ≥ 2.0

---

### 步驟 2：把專案資料夾複製到 NAS

用電腦連線 NAS 的共用資料夾（Finder → 前往 → 連接伺服器），把整個 `業務信件開發軟體` 資料夾拖進去，例如放在：

```
/share/homes/admin/mailer/
```

記住這個路徑，後面會用到。

---

### 步驟 3：建立 `.env` 設定檔

在 NAS 上的專案資料夾裡，把 `.env.example` 複製一份，改名為 `.env`，然後填入以下兩項（其餘可以不動）：

```
OPENAI_API_KEY=sk-proj-你的OpenAI金鑰
SECRET_KEY=隨便一串你自己記得的字，例如 yichian2024xyz
```

> ⚠️ `.env` 檔案不要分享給任何人，裡面有金鑰。

---

### 步驟 4：在 NAS 上建立並啟動容器

打開 NAS 的 SSH 終端機（或用 Container Station 的介面），進入專案資料夾：

```bash
cd /share/homes/admin/mailer
```

因為 NAS 是 ARM 架構，需要直接在 NAS 上建置映像檔（約 5～10 分鐘）：

```bash
docker-compose up -d --build
```

若 NAS 已有網路可直接從 Docker Hub 拉取 amd64 版本（測試用）：

```bash
docker-compose pull
docker-compose up -d
```

看到 `Started` 或 `Running` 就代表成功。

---

### 步驟 5：確認系統正在運作

在 NAS 的瀏覽器或你的電腦瀏覽器輸入：

```
http://NAS的IP位址:8000
```

看到登入頁面就代表部署成功 🎉

預設帳號：`admin`  
預設密碼：`admin1234`  
**請登入後立刻到「帳號管理」→ 重設密碼**

---

## 日常維護

### 查看系統是否在跑

```bash
docker-compose ps
```

Status 欄位顯示 `Up` 代表正常。

---

### 重新啟動系統

```bash
docker-compose restart
```

---

### 停止系統

```bash
docker-compose down
```

> 資料不會遺失，因為資料庫存在 `./data/` 資料夾裡，不在容器內。

---

### 更新程式碼後重新部署

```bash
docker-compose down
docker-compose up -d --build
```

---

### 備份資料

只需要備份 `data/` 資料夾，裡面的 `mailer.db` 就是所有資料（帳號、信箱設定、發送記錄）。

```bash
cp data/mailer.db data/mailer_備份_$(date +%Y%m%d).db
```

---

## 常見問題排查

### ❌ 打開網頁顯示「無法連線」

1. 確認容器有在跑：`docker-compose ps`
2. 確認埠號沒有衝突：QNAP 上若 8000 被佔用，可在 `docker-compose.yml` 把 `"8000:8000"` 改成 `"8080:8000"`，然後用 `:8080` 連線

---

### ❌ 登入後 AI 寫信出現錯誤

1. 確認 `.env` 裡的 `OPENAI_API_KEY` 正確（沒有多餘空格）
2. 確認 OpenAI 帳號有餘額：[platform.openai.com/usage](https://platform.openai.com/usage)

---

### ❌ 寄信失敗 / SMTP 連線錯誤

**Gmail：**
- 必須使用「應用程式密碼」，不是登入密碼
- 步驟：Google 帳戶 → 安全性 → 兩步驟驗證（先開啟）→ 應用程式密碼 → 產生

**阿里企業信箱：**
- 需請 IT 在後台開啟「三方客戶端 SMTP」
- 使用「三方客戶端安全密碼」，不是登入密碼
- 主機：`smtp.qiye.aliyun.com`，埠號：`465`

---

### ❌ ARM 架構映像檔錯誤（只在 QNAP 出現）

若 Container Station 顯示架構不相容：

```bash
# 在有 Docker buildx 的電腦上執行多架構打包
docker buildx create --use
docker buildx build \
  --platform linux/amd64,linux/arm/v7 \
  -t yichian/mailer:latest \
  --push .
```

然後在 NAS 改用 pull 而非 build：

```yaml
# docker-compose.yml 改為
image: yichian/mailer:latest
# 移除 build: . 這行
```

```bash
docker-compose pull
docker-compose up -d
```

---

### ❌ 容器跑起來但資料消失了

確認 `docker-compose.yml` 裡有正確掛載 volume：

```yaml
volumes:
  - ./data:/app/data
```

且 `./data/` 資料夾存在於專案目錄中（系統第一次啟動會自動建立）。

---

## 多架構說明（技術參考）

| 架構 | 適用機器 | 支援狀態 |
|------|---------|---------|
| `linux/amd64` | 一般 x86 電腦、雲端伺服器 | ✅ |
| `linux/arm/v7` | QNAP TS-431X2（32-bit ARM） | ✅ Python 官方支援 |

使用的元件（Python 3.11-slim、FastAPI、SQLite）均有官方 ARM 32-bit 支援，無需額外處理。

---

_最後更新：2026-06_
